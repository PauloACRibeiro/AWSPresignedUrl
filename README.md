This external library exposes actions to generate pre-signed AWS S3 URLs that OutSystems apps can call at runtime. 
Supply the bucket, object key, and expiration (and content type for uploads) to obtain secure GET or PUT URLs, with all AWS credential handling managed inside the library so your ODC apps can upload or download 
files directly to S3.
